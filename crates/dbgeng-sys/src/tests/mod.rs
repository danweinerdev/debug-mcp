//! Unit tests for `dbgeng-sys` (dedicated `src/tests/` module per CLAUDE.md — never inline
//! `#[cfg(test)]` next to the code). Live-engine coverage lives in `tests/engine.rs`.

mod callbacks;
mod error;
